
/**
* @description The function `saveButtonCallback()` collects all flight data using
* the function `collectAllFlightData()`.
*/
function saveButtonCallback(){
    let data = collectAllFlightData();
}

/**
* @description This function doesn't do anything since it is empty.
*/
function collectAllFlightData() {
    let data = []
    

}